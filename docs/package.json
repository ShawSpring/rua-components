{
  "name": "docs",
  "private": "true",
  "type": "module",
  "scripts": {
    "dev": "SHOW_ALL_COMPONENT_DEMOS=true vite",
    "build-serve": "rimraf dist && vite build  && serve -s dist",
    "ssr-docs": "rimraf dist && vite-pages ssr && serve dist",
    "build": "vite build"
  },
  "dependencies": {
    "react-router-dom": "^6.21.3",
    "rua-components": "workspace:*"
  },
  "devDependencies": {
    "@mdx-js/react": "^3.0.0",
    "rimraf": "^5.0.5",
    "serve": "^14.2.3",
    "vite-pages-theme-doc": "^5.0.0",
    "vite-plugin-react-pages": "^5.0.0"
  }
}
